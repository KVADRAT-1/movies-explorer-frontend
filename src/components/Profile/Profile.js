import './Profile.css';

function Profile() {
  return (
    <div className="Profile">
      → → → Profile ← ← ←
    </div>
  );
}

export default Profile;

// Profile — компонент страницы изменения профиля.
